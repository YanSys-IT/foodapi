Тестовое задание backend разработчик UTF.tech
Стек Django\DRF

Даны модели "Категория Блюд" и "Блюдо" для ресторана.
Даны сериализаторы.

В выборку попадают только Блюда у которых is_publish=True.
Если в категории нет блюд (или все блюда данной категории имеют is_publish=False) - не включается в выборку.

Запрос в БД сделаны Django ORM

Написан View который возвращает для API 127.0.0.1/api/v1/foods/ JSON требуемого формата

